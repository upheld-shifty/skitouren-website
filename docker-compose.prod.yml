# This file is no longer needed.
# docker-compose.yml is now the production configuration.
#
# Production:  docker compose up -d --build
# Development: docker compose -f docker-compose.dev.yml up --build
